// Transforma la siguiente función a una arrow function de una línea.

/*
let suma = function(a, b){
return a + b
};
*/

// Arrow Function
let suma = (a, b) => a + b;

console.log(`Arrow function:`, suma(1, 2) );